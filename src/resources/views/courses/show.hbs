<div class="page-section course-detail-container">
    <div class="row">
        <!-- ACTION SIDEBAR -->
        <div class="col-lg-3 mb-3">
            <button class="btn btn-primary w-100 course-action-btn">
                Học ngay
            </button>
        </div>

        <!-- COURSE CONTENT -->
        <div class="col-lg-9">
            <h2 class="course-title">{{course.name}}</h2>

            <div class="course-video">
                <iframe 
                    src="https://www.youtube.com/embed/{{course.videoId}}" 
                    title="{{course.name}}"
                    allowfullscreen>
                </iframe>
            </div>

            <p class="course-description">
                {{course.description}}
            </p>
        </div>
    </div>
</div>
